{
  "name": "data-forge",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"pnpm --filter electron dev\" \"pnpm --filter render-view dev\"",
    "build": "pnpm --filter electron build && pnpm --filter render-view build",
    "start": "pnpm --filter electron start"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
